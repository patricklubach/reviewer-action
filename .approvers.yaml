check_on: branch_name

rules:
  - regex: ^feature/
    count: 1
    approvers:
      - user:patricklubach
      - user:racoon63
  - regex: "^dependabot/"
    approvers:
      - user:patricklubach
  - default: true
    approvers:
      - user:patricklubach
