check_on: branch_name
set_approvers: true

rules:
  - regex: ^feature/
    count: 1
    approvers:
      - user:racoon63
  - regex: "^dependabot/"
    approvers:
      - user:patricklubach
  - default: true
    approvers:
      - user:patricklubach
